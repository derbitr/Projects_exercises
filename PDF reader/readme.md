Created a PDF reader for my study schedule, used the os, logging, pandas and pdfplamber for this mini project.I used the os librarie for find the pdf inside of windows PATH, the logging for register the try/except and if/else commands, used the pandas to transform the pdf in a dataframe with columns and lines with the pdfplamber librarie for catch the text inside from pdf and translated to pandas transform into a DataFrame