For my new learn concept, i wanted to do something with FASTAPI, then i made a system web with 3 principals files using os, logging, fastpi, uvicorn, pydantic and datetime librares, in these 3 files i splited into a SQLITE data bank for create table and insert values inside, an CRUD for learning some financial concepts and variables and in the main file i made a engine with FASTAPI (api restful) and after many tries i sucessfull made a script for running in another setups (mac/linux,windows) with a INTERFACE setup for test using POST and GET.